dbcreation:
  image:
    pullPolicy: "IfNotPresent"
{% if commercial.enabled %}
    repository: "{{commercial.registry.url}}/vanillastack/spilo-12"
{% else %}
    repository: "registry.opensource.zalan.do/acid/spilo-12"
{% endif %}
    tag: "1.6-p3"
# fullnameOverridestring: ""
guacamole:
  image:
{% if commercial.enabled %}
    repository: "{{commercial.registry.url}}/vanillastack/guacamole"
{% else %}
    repository: "vanillastack-guacamole/guacamole"
{% endif %}
    tag: "{{ guacamole.imageTag }}"
guacd:
  image:
{% if commercial.enabled %}
    repository: "{{commercial.registry.url}}/vanillastack/guacd"
{% else %}
    repository: "vanillastack-guacamole/guacd"
{% endif %}
    tag: "{{ guacamole.imageTag }}"
# imagePullSecretslist: []
ingress:
  annotations:
    nginx.ingress.kubernetes.io/proxy-body-size: 1024m
    nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
    cert-manager.io/cluster-issuer: "{{ tls.issuerName }}"
    kubernetes.io/tls-acme: "true"
    nginx.ingress.kubernetes.io/app-root: /guacamole
  enabled: true
  host: "{{ guacamole.coreDomain }}"
  # tls: []
# nameOverride: ""
# nodeSelector: {}
# podSecurityContext: {}
postgres:
  database: "guacamole"
  hostname: "guacamole-postgresql"
  password: "{{ postgrespassword.stdout }}"
  port: "5432"
  user: "guacamole"
replicaCount: 1
# resources: {}
# securityContext: {}
service:
  port: 80
  type: "ClusterIP"
serviceAccount:
  create: true
  # name: nil
# tolerations: []
