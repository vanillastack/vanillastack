apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  annotations:
    kubernetes.io/ingress.class: nginx
    cert-manager.io/cluster-issuer: certmanager-selfsigned
{% if rook.cluster.dashboard.ingress.tls %}
    cert-manager.io/cluster-issuer: "{{rook.cluster.dashboard.ingress.issuerName}}"
    kubernetes.io/tls-acme: "true"
{% endif %}
    nginx.ingress.kubernetes.io/backend-protocol: HTTPS
    nginx.ingress.kubernetes.io/server-snippet: |
      proxy_ssl_verify off;
  name: rook-ceph-mgr-dashboard
  namespace: rook-ceph
spec:
  rules:
  - host: {{rook.cluster.dashboard.ingress.domain}}
    http:
      paths:
      - backend:
          service:
            name: rook-ceph-mgr-dashboard
            port:
              name: https-dashboard
        path: /
        pathType: Prefix
  tls:
  - hosts:
    - {{rook.cluster.dashboard.ingress.domain}}
    secretName: tls-dashboard-endpoint
