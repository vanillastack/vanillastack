---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: "mpaas-customer-{{sa.name}}"
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: "mpaas-customer-global-role-sa-{{sa.name}}"
subjects:
- kind: ServiceAccount
  name: "{{sa.name}}"
  namespace: "{{sa.namespace}}"
